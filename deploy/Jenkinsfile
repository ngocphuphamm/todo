pipeline{

    stages{
        stage("verify toolong"){
            steps{
                sh '''
                  cd ..
                  docker-compose up -d --build
                '''
            }
        }
    }
}
